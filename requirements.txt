# Phase 1 application dependencies
-r backend/requirements.txt

# Device acquisition dependencies remain isolated until the firmware/data contract is finalized.
# -r backend/requirements_acquisition.txt
